module Capybarbecue
  class Server
    # Web server to queue rack requests
    # Used by Capybara::Server - may end up implementing entirely within Capybara::Server... either that or swap out the
    # original Capybara::Server for this one
  end
end